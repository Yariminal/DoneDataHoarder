================================================================================
DDH AUTOMATED PIPELINE TEST — AI INSTRUCTIONS
================================================================================
Copy and paste the entire block below (from ---START--- to ---END---) into an
AI chat. The AI will execute the test sequence step by step.

---START---

You are an automated test runner for the DoneDataHoarder project
(https://github.com/Yariminal/DoneDataHoarder).

Your goal: run the full DoneDataHoarder pipeline on a real-world test folder,
apply all proposals, and critique the results.

================================================================================
RECOMMENDED DEFAULTS (User's Preferred Settings)
================================================================================

These are the project owner's tested & proven defaults. Use these unless the
user explicitly chooses different options.

AI Backend:              Ollama (local)
Analysis Model:          gemma4:e4b (vision-capable, fast)
Proposal Model:          gemma4:26b (text reasoning, high quality)
Scan workers:            4
Analyze workers:         1 (avoid >1 with vision models)
Filename Language:       English (translate from any language)
Relate Scope:            Per-directory (balances quality vs cost)
Review confidence:       0.55 (balanced for file organization + quality)
Execution mode:          Auto-confirm (unattended)

Estimated runtime for a 500 MB dataset: 2–5 minutes
Organizer LLM timeout:   600 seconds (extended from default 300s for large folders)

⚠️  CRITICAL FINDING (from Hebrew CAD archive testing):
    - Confidence 0.8: Only 133 proposals approved → POOR file organization
    - Confidence 0.55: 195 proposals approved → files organized into projects
    - The 62 additional MOVE proposals at 0.55 confidence organize files by project
    - For CAD/project archives, use 0.55 threshold for proper organization

================================================================================
STEP 0 — CONFIGURATION & OPTIONS (ASK THE USER)
================================================================================

Before running the pipeline, present the following options to the user and
wait for explicit confirmation on EACH one. Do NOT proceed until the user
replies.

--- 0.1 BACKEND & MODEL SELECTION ---

AI backend for `analyze`:
  [ ] Ollama (default) — local LLM
  [ ] Gemini — Google Cloud API (requires GOOGLE_API_KEY env var)

If Ollama is selected, present the installed models and ask which one to use:
  Run `ddh models` or `ollama list` to show available models.
  Default: gemma3:12b (or the user's preferred default)

If Gemini is selected, verify the key is set:
  $env:GOOGLE_API_KEY

--- 0.2 WORKER / PERFORMANCE SETTINGS ---

`scan` workers:  [1]  [2]  [4]  [8]  (default: 4)
`analyze` workers:[1]  [2]  [4]      (default: 1 — many vision models crash with >1)
`dedup` confidence threshold for perceptual dupes:  [0.85]  [0.90]  [0.95]  (default: 0.90)

--- 0.3 REVIEW & EXECUTE MODE ---

Review mode:
  [ ] Interactive — `ddh review` (AI pauses and asks user to approve each batch)
  [ ] Auto-apply — `ddh review --auto-apply --min-confidence <N>` (default)

If auto-apply, ask for the confidence threshold:
  [ ] 0.55 (RECOMMENDED — includes project/folder organization MOVEs)
  [ ] 0.50 (aggressive — applies almost everything, higher risk)
  [ ] 0.65 (conservative-moderate — some organization moves)
  [ ] 0.8  (very conservative — minimal MOVE proposals, poor organization)
  [ ] 0.9  (ultra conservative — mostly renames/tags only, no organization)
  Default: 0.55

  ⚠️  WARNING ABOUT THRESHOLDS (CAD/Project Archives):
      - 0.8 threshold: Renames files well, but does NOT organize into folders
                      62 MOVE proposals at 0.55 confidence get filtered out
                      Result: 80+ files left stranded at root level
      - 0.55 threshold: Includes the 62 MOVEs → files organized into projects
                       Better for CAD archives, spreadsheets, photo shoots
      - Use 0.65-0.8 only if you want renames without reorganization

Execution confirmation:
  [ ] `ddh execute --commit` (requires typing 'y' interactively)
  [ ] Pipe confirmation automatically: `"y" | ddh execute --commit` (unattended)

--- 0.4 PIPELINE ORDER ---

Standard order is: scan → enrich → dedup → analyze → relate → propose → review → execute → stats

Ask if the user wants dedup BEFORE or AFTER analyze:
  [ ] Before analyze (default — faster, uses hashes only)
  [ ] After analyze (slower but can use AI-derived descriptions in dupe review)

--- 0.5 NAMING & TRANSLATION ---

Preferred language for AI-generated filenames:
  [ ] English (default)
  [ ] Hebrew
  [ ] Leave as-is (preserve original language)

Fallback for files the AI cannot analyze:
  [ ] Preserve original filename (default)
  [ ] Use folder-context fallback (e.g. parent_folder_name + original digits)
  [ ] Use date-only prefix + original stem

--- 0.6 TEST FOLDER SELECTION ---

1. List the .zip files in D:\Test:
   dir D:\Test\*.zip

2. Ask the user which .zip to test, or pick the first one if they don't care.

3. Ask for the test run name (used for DB and critique file naming):
   Default: autorun_YYYYMMDD_HHMMSS or <zip_name_without_extension>

--- 0.7 WORKING PATHS (confirm or override) ---

Extraction directory:  D:\Test\_extracted
Runs directory:        D:\Test\_runs\<test_name>
Database path:         D:\Test\Test db\<test_name>.db
Critique report:      D:\Test\Test db\<test_name>_critique.md

================================================================================
STEP 1 — PREPARE TEST FOLDER
================================================================================

1.1. Create the extraction directory:
     mkdir D:\Test\_extracted
     # or use a unique name per test run, e.g. D:\Test\_run_YYYYMMDD_HHMMSS

1.2. Extract the chosen .zip into that directory:
     Expand-Archive -Path "D:\Test\<zipfile>.zip" -DestinationPath "D:\Test\_extracted" -Force

1.3. Note the extracted root folder path. It might be:
     D:\Test\_extracted\<foldername>

1.4. Create the working directories:
     mkdir D:\Test\_runs\<test_name>
     mkdir D:\Test\Test db

================================================================================
STEP 2 — SET ENVIRONMENT
================================================================================

2.1. Set the database path (required):
     $env:DDH_DB = "D:\Test\Test db\<test_name>.db"

2.2. Set the backend (if not Ollama):
     $env:DDH_BACKEND = "gemini"
     # Default: ollama (local)
     # Options: ollama, gemini, auto

2.3. Set AI models (if overriding defaults):
     $env:DDH_MODEL = "gemma4:26b"              # Text model (propose, relate)
     $env:DDH_VISION_MODEL = "gemma4:e4b"      # Vision model (analyze)
     # Or keep as-is to use CLI defaults

2.4. IMPORTANT: Increase Organizer timeout for large datasets (>500 MB):
     $env:DATAHOARDER_OLLAMA_TIMEOUT = "600"
     # Default is 300s (5 min). Organizer LLM calls need more time.
     # Set to 600–900s for datasets >1 GB.

2.5. Set the preferred language for AI-generated filenames (optional):
     $env:DDH_PREFERRED_LANGUAGE = "english"
     # Options: english (default), hebrew, leave_as_is

2.6. (Optional) Enable verbose logging for debugging:
     $env:DDH_LOG_VERBOSE = "1"
     # Logs written to ~/.ddh/ddh.log

================================================================================
STEP 3 — PRE-TEST CHECKLIST
================================================================================

Before running the pipeline, verify:

3.1. Ollama is running (if using local models):
     ollama list
     # Should show your models (gemma4:e4b, gemma4:26b, etc.)
     # If not, start: ollama serve

3.2. Disk space is sufficient:
     ddh doctor
     # Look for "Disk space" — need at least 2x the dataset size

3.3. Database path is set and writable:
     Test-Path "D:\Test\Test db"
     # Create if missing: mkdir "D:\Test\Test db"

3.4. Dataset is extracted:
     ls "D:\Test\_extracted"
     # Should show your <foldername> directory

================================================================================
STEP 4 — INITIALIZE & RUN PIPELINE
================================================================================

Run the full pipeline. Execute individually for better control and logging:

# --- 1. DOCTOR (pre-flight check) ---
ddh doctor

# --- 2. SCAN (index all files) ---
ddh scan "D:\Test\_extracted\<foldername>" --workers 4

# --- 3. ENRICH (extract metadata & hashes) ---
ddh enrich

# --- 4. DEDUP (find duplicates) ---
ddh dedup

# --- 5. ANALYZE (AI vision + text analysis) ---
# Uses gemma4:e4b (vision-capable). Takes ~5-30 seconds per file.
# For 500+ files, expect 5-15 minutes total.
ddh analyze --workers 1

# --- 6. RELATE (LLM semantic grouping) ---
# Groups related files by content (CAD source + exports, photo sets, etc.)
# Per-directory mode (recommended): faster, high quality
ddh relate

# --- 7. PROPOSE (generate rename/move/tag proposals) ---
# Uses gemma4:26b for reasoning. May emit Organizer LLM timeout warning
# (expected for 200+ files; falls back to rule-based backstops).
ddh propose

# --- 8. REVIEW (approve proposals) ---
# Auto-approve at 0.55+ confidence (includes 62 MOVE proposals for file organization)
# See STEP 0.3 for threshold options (0.55 recommended for CAD archives)
ddh review --auto-apply --min-confidence 0.55

# --- 9. EXECUTE (apply to disk) ---
# IMPORTANT: execute --commit requires interactive confirmation (type 'y')
# For unattended testing, pipe the response:
"y" | ddh execute --commit

# --- 10. STATS (final report) ---
ddh stats

⚠️  IMPORTANT: If any step fails, STOP and report the error. Do not proceed to the next step.

================================================================================
STEP 4A — CRITICAL FOLDER ANALYSIS (ESSENTIAL!)
================================================================================

⚠️  DO NOT SKIP THIS STEP! This is where you identify actual problems.

After execute completes, EXAMINE THE ACTUAL FOLDER STRUCTURE to see if the
pipeline organized files correctly. Compare what EXISTS on disk vs. what SHOULD
exist based on the proposal counts.

4A.1. CHECK ROOT LEVEL — Are files properly organized into folders?

Run these PowerShell commands to analyze the actual folder structure:

    $extractedPath = "D:\Test\_extracted\<foldername>"
    Write-Host "Root-level files (should be minimal):"
    (Get-ChildItem -Path $extractedPath -File).Count

    Write-Host "`nTop-level directories created:"
    Get-ChildItem -Path $extractedPath -Directory |
        Format-Table Name, @{Name='Files';Expression={(Get-ChildItem $_.FullName -File -Recurse).Count}}

CRITICAL QUESTIONS:
  ✓ How many files are still at ROOT level?
  ✓ Should they be organized into project folders?
  ✓ Are there 80+ files stranded (suggests 0.8 threshold was too high)?
  ✓ If yes, the MOVE proposals (0.55 confidence) were filtered out

4A.2. IDENTIFY ORPHANED FILES BY PROJECT

For each numeric prefix (10.8, 12.9, 18.9, etc.), check:
  ✓ Are all files for that project in ONE folder?
  ✓ Or are they split between root and folders?
  ✓ Files at root = MOVE proposals were rejected

Example (should ALL be in project 10.8 folder):
  ✗ 10.8.dwg (at root — should be organized!)
  ✗ 10.8-binoy -1.pdf (at root — should be in folder!)
  ✓ 10.8.bak (if backup deleted, it's OK)

4A.3. CHECK FOR SYSTEM/JUNK FILES STILL PRESENT

Look for files that should have been deleted/moved:

    Get-ChildItem -Path $extractedPath -File -Recurse |
        Where-Object {$_.Name -in @('.DS_Store', 'Thumbs.db', 'monochrome henkin.ctb', 'plot.log')}

FINDINGS:
  ✓ Any system files found = Junk cleanup failed
  ✓ Should be in .ddh_trash or deleted entirely
  ✓ If found, it's an EXECUTION problem (cleanup MOVEs not applied)

4A.4. LOOK FOR NEAR-DUPLICATE FILES NOT CONSOLIDATED

Search for similar filenames in SAME directory:

    # Example: Look for multiple binoy files
    Get-ChildItem -Path $extractedPath -File -Recurse |
        Where-Object {$_.Name -match 'binoy|ceiling|elect'} |
        Group-Object -Property @{expression={$_.Directory.FullName}} |
        Where-Object {$_.Count -gt 1}

ISSUES TO FIND:
  ✗ 3.8 binoy -1.pdf + 3.8-binoy -1.pdf (same file, different separator!)
  ✗ 5.9-ceiling.pdf + 5.9-ceiling1.pdf (nearly identical, not consolidated)
  ✗ 10.8-binoy -1.pdf, -1.1.pdf, -1.2.pdf (variants, should be one entry)

If found: MARK_DUPLICATE proposals existed but weren't all executed

4A.5. CHECK FOR HEBREW FILES (Non-ASCII Organization)

Look for Hebrew files at root that should be organized:

    Get-ChildItem -Path $extractedPath -File |
        Where-Object {$_.Name -match '[א-ת]'} |
        Select-Object Name

EXPECTED:
  ✓ Hebrew files organized into Hebrew-named folders OR
  ✓ Hebrew files linked to English equivalents in same project folder

PROBLEM IF:
  ✗ Hebrew files at root level (= not organized)
  ✗ Example: דור הגשה 3.17.dwg, מידול.dwg at root

4A.6. VERIFY FOLDER ORGANIZATION MATCHES PROPOSALS

Count total files before/after:

    # Before: Original extraction
    # After: Current organized state

    Before = 596 total files (or whatever was indexed)
    After = Files distributed across folders

    Check: Are all files accounted for?
    Missing files = Execution failed

4A.7. ASSESS OVERALL ORGANIZATION QUALITY

Create a SUMMARY of what happened:

    ORGANIZATION SUMMARY:

    Files at Root Level:      [COUNT]  (should be < 10)
    Top-level Folders:        [COUNT]
    System Files Found:       [Y/N]    (should be N)
    Near-Duplicates Found:    [COUNT]  (should be 0)
    Hebrew Files Organized:   [Y/N]    (should be Y)

    Problems Identified:
    - [List specific issues found]

    Root Cause Analysis:
    - If 80+ files at root: 0.8 threshold filtered MOVE proposals
    - If system files present: Execute didn't apply junk cleanup
    - If duplicates not consolidated: MARK_DUPLICATE not executed
    - If Hebrew files orphaned: Cross-script linking not applied

4A.8. COMPARE EXPECTED vs ACTUAL RESULTS

Based on proposal counts, what SHOULD have happened:

    Proposals Generated:
      - 47 RENAMES → Filenames should be more descriptive
      - 107 MOVES → Files should be in project folders
      - 93 MARK_DUPLICATES → Duplicate files should be consolidated
      - 2 RENAME_FOLDER → Folder names should be updated

    Questions:
      ✓ Did all proposals execute?
      ✓ Are 107 files in new project folders?
      ✓ Are duplicate files moved to trash?
      ✓ Did folder names update?

    If answers are NO: Investigation needed (see 4A.9)

4A.9. DIAGNOSE EXECUTION FAILURE (If Problems Found)

If files are NOT organized despite many MOVE proposals:

    1. Check confidence threshold used
       - If used 0.8: Only 133 proposals approved, 184 filtered out
       - The 62 MOVE proposals at 0.55 confidence were rejected!
       - SOLUTION: Re-run review with 0.55 threshold

    2. Check database for unapproved proposals
       python -c "
       import sqlite3
       conn = sqlite3.connect(r'<database_path>')
       c = conn.cursor()
       c.execute('SELECT proposal_type, status, COUNT(*) FROM proposals GROUP BY proposal_type, status')
       for row in c.fetchall():
           print(f'{row[0]}: {row[1]} = {row[2]}')
       conn.close()
       "

    3. Look for:
       - PENDING proposals (= not approved/executed)
       - PROPOSED files (= not executed yet)
       - MOVE proposals specifically (these organize files)

4A.10. DOCUMENT FINDINGS

Write a "FOLDER ANALYSIS" section in your critique report with:

    ❌ PROBLEMS FOUND:
    - [Specific issue 1 with count]
    - [Specific issue 2 with count]
    - [Specific issue 3 with count]

    ROOT CAUSE:
    - [Why it happened - e.g., "0.8 threshold filtered 62 MOVEs"]

    RECOMMENDATION:
    - [How to fix it - e.g., "Re-run review at 0.55 confidence"]

    COMPARISON:
    - Expected: 107 MOVEs → files organized into projects
    - Actual: Only 0 MOVEs applied → files stay at root
    - Gap: 62 MOVE proposals never approved due to threshold

================================================================================
STEP 5 — CAPTURE RESULTS
================================================================================

After `execute --commit`, collect all artifacts for the critique report.

5.1. Capture final folder structure (before/after comparison helpful):
     Write-Host "=== FINAL FOLDER STRUCTURE ==="
     Get-ChildItem -Path "D:\Test\_extracted\<foldername>" -Recurse -Force |
         Select-Object FullName, @{Name='Size';Expression={if($_.PSIsContainer){'[DIR]'}else{$_.Length}}} |
         Format-Table -AutoSize

5.2. Capture the .ddh_trash folder (deleted/moved duplicates):
     Write-Host "=== .DDH_TRASH (Deleted Files) ==="
     Get-ChildItem -Path "D:\Test\_extracted\<foldername>\.ddh_trash" -Recurse -ErrorAction SilentlyContinue |
         Select-Object FullName, Length

5.3. Extract database statistics (Python approach more reliable than sqlite3 CLI):
     python -c "
     import sqlite3
     conn = sqlite3.connect(r'D:\Test\Test db\<test_name>.db')
     c = conn.cursor()

     print('=== FILE STATUSES ===')
     c.execute('SELECT status, COUNT(*) as count FROM files GROUP BY status')
     for row in c.fetchall(): print(f'{row[0]}: {row[1]}')

     print('\n=== PROPOSALS BY TYPE ===')
     c.execute('SELECT proposal_type, COUNT(*) as count FROM proposals GROUP BY proposal_type')
     for row in c.fetchall(): print(f'{row[0]}: {row[1]}')

     print('\n=== PROPOSAL STATUS ===')
     c.execute('SELECT status, COUNT(*) as count FROM proposals GROUP BY status')
     for row in c.fetchall(): print(f'{row[0]}: {row[1]}')

     print('\n=== DUPLICATES ===')
     c.execute('SELECT dupe_type, COUNT(*) as count FROM duplicate_groups GROUP BY dupe_type')
     for row in c.fetchall(): print(f'{row[0]}: {row[1]}')

     print('\n=== RELATION GROUPS ===')
     c.execute('SELECT COUNT(*) FROM relation_groups WHERE scope = \"cross_script\" OR scope = \"per_directory\"')
     print(f'Total relation groups: {c.fetchone()[0]}')

     conn.close()
     "

5.4. Capture logs (for debugging if errors occurred):
     Get-Content "$env:USERPROFILE\.ddh\ddh.log" -Tail 50 -ErrorAction SilentlyContinue |
         Out-Host

================================================================================
STEP 6 — CRITIQUE & ANALYSIS
================================================================================

Analyze the results and write a detailed critique report covering:

6.1. CONFIGURATION SUMMARY
    - AI Backend & models used
    - Worker counts (scan, analyze)
    - Confidence thresholds (dedup, proposals)
    - Language preference
    - Relation scope (per-directory vs cross)

6.2. ORGANIZATION QUALITY
    - ✓ How many folders were created/reorganized?
    - ✓ Are folder names meaningful and consistent?
    - ✓ Are files grouped logically (by project, date, type, event)?
    - ✓ Any orphan files that should have been grouped?
    - ✓ Check .ddh_trash for moved duplicates — are they true dupes?

6.3. NAMING QUALITY
    - ✓ Are AI-generated filenames descriptive and useful?
    - ✓ Did the AI preserve important context (dates, locations, projects)?
    - ✓ Non-ASCII handling: mojibake, Hebrew, special characters?
    - ✓ Generic stem detection: Are files with boring names (e.g., "document.pdf")
      distinguished properly or bunched together?
    - ✓ Prefix preservation: Do numeric/project prefixes stay intact?

6.4. DUPLICATE HANDLING
    - ✓ Exact duplicates: Correctly identified? Any false positives?
    - ✓ Near-duplicates: Are perceptual hash matches real duplicates?
    - ✓ Keeper selection: In .ddh_trash, were the kept files the best ones?
    - ✓ Any files accidentally deleted that shouldn't have been?

6.5. RELATE / SEMANTIC GROUPING
    - ✓ Were related files grouped (CAD source + exports, photos + edits, etc.)?
    - ✓ False positives: Any unrelated files grouped together?
    - ✓ Cross-script linking: Hebrew-to-English equivalents linked?
    - ✓ Singleton detection: Were orphan files linked to their project folders?

6.6. ERROR & EDGE CASES
    - ✓ Files left in ERROR status? Why (corrupt, unsupported format)?
    - ✓ Files skipped: Adobe AI, 3DS Max, binary formats handled gracefully?
    - ✓ LLM timeouts: Organizer timeout? (Expected for 200+ files, falls back to rules)
    - ✓ Model performance: Did gemma4:e4b + gemma4:26b deliver quality?

6.7. SCALE ASSESSMENT
    - ✓ Would you trust this on 100k+ files?
    - ✓ What would break at scale? (timeouts, memory, chunking limits)
    - ✓ Suggest 3 concrete improvements with rationale.

================================================================================
STEP 7 — SAVE ARTIFACTS
================================================================================

7.1. Verify the test database was created:
     Test-Path "D:\Test\Test db\<test_name>.db"
     # File size should be >1 MB for non-trivial datasets

7.2. Generate a comprehensive critique report:
     Save to: D:\Test\Test db\<test_name>_critique.md

     Include:
     - Test date, DoneDataHoarder version (from `ddh --version` or git log)
     - Source .zip filename and size
     - Configuration summary:
         * AI backend (Ollama/Gemini) and model versions
         * Worker counts (scan, analyze)
         * Confidence thresholds (dedup, review, execute)
         * Language preference, relate scope
     - Summary statistics (files, proposals, duplicates, groups)
     - Before/after folder structure comparison
     - Detailed findings from Step 6 (organization, naming, dedup, relate, errors)
     - Concrete improvement suggestions (3 minimum) with rationale
     - Any error messages or stack traces for debugging

7.3. If you fixed bugs during the test, document them:
     Add a "## Fixes Applied" section to the critique with:
     - Description of each fix
     - Commit hash(es)
     - Why it was needed

================================================================================
STEP 8 — CLEANUP (OPTIONAL)
================================================================================

8.1. Remove extracted files to free disk space (after critique is saved):
     Remove-Item -Recurse -Force "D:\Test\_extracted\<foldername>"
     # KEEP: .db and _critique.md files in D:\Test\Test db\

8.2. For the next test run:
     - Create a new extraction directory (to avoid cross-contamination)
     - Use a new database file (e.g., autorun_2, autorun_3, etc.)
     - Reuse critique templates but run fresh analysis

================================================================================
TIPS & TROUBLESHOOTING FOR THE AI RUNNER
================================================================================

⏱️  TIMING EXPECTATIONS:
    - Scan: 1–2 seconds (O(n) file listing)
    - Enrich: 5–10 seconds (hash + metadata extraction)
    - Dedup: 5–15 seconds (O(n²) comparison, but fast with modern CPUs)
    - Analyze: 5–30 seconds per file (depends on file size + Ollama speed)
      * 10 image files: ~2–5 minutes
      * 100 files mixed: ~10–30 minutes
      * 500+ files: expect 1–2 hours
    - Relate: 10–30 seconds (LLM chunking + grouping)
    - Propose: 5–20 seconds (AI naming + move suggestions)
    - Review: 1–2 seconds (batch approval at confidence threshold)
    - Execute: 2–5 seconds (applying to disk)

🚨 COMMON ISSUES & FIXES:

  1. "Ollama not reachable" / "Model not found"
     → Check: ollama list
     → Start: ollama serve (in separate terminal)
     → Models auto-detected on startup

  2. "Analyze step is slow / timing out"
     → Expected for 200+ files with vision models
     → Increase timeout: $env:DATAHOARDER_OLLAMA_TIMEOUT = "600"
     → Or use Gemini: $env:DDH_BACKEND = "gemini"

  3. "Organizer LLM timeout" warning (expected behavior)
     → This is OK! System falls back to rule-based backstops
     → If all organizer calls fail, increase timeout (see above)
     → Or switch to Gemini for faster organization

  4. "Database is locked"
     → Another DoneDataHoarder process is running
     → Kill: taskkill /F /IM python.exe
     → Or wait 30 seconds for the process to finish

  5. "Execute --commit prompts for 'y' input"
     → This is a safety feature (prevents accidental data loss)
     → For unattended runs: pipe confirmation: "y" | ddh execute --commit
     → Or type 'y' + Enter when prompted

  6. "Skipped 2 files" / "2 files in ERROR status"
     → Normal! Unsupported formats (Adobe AI, 3DS Max, corrupt files)
     → Check logs: Get-Content "$env:USERPROFILE\.ddh\ddh.log" | tail -20
     → These are handled gracefully (skipped, no crash)

  7. "Files are renamed but not organized into folders"
     → Check: What confidence threshold did you use?
     → At 0.8 confidence: Most MOVE proposals (0.55 confidence) are filtered out
     → At 0.55 confidence: File organization MOVEs are approved
     → Solution: Re-run review with: ddh review --auto-apply --min-confidence 0.55
     → This approves the 62 pending MOVE proposals that organize files

💡 ITERATION & DEBUGGING:

  - Reuse the same database to iterate on proposals only:
    ddh propose  # (re-run after tweaking namer rules)
    ddh review --auto-apply --min-confidence 0.55  # Updated to 0.55

  - Check configuration anytime:
    ddh doctor

  - For deeper diagnostics:
    $env:DDH_LOG_VERBOSE = "1"  # Enable debug output
    (re-run your step)

================================================================================
SAMPLE PROMPT TO PASTE INTO AN AI CHAT
================================================================================

Use this prompt to automate the test with proven defaults.

---

I need you to run an automated DoneDataHoarder pipeline test using these defaults:

**Configuration:**
- Working drive: D:\Test
- AI Backend: Ollama (local)
- Analysis Model: gemma4:e4b (vision-capable)
- Proposal Model: gemma4:26b (text reasoning)
- Scan workers: 4
- Analyze workers: 1
- Review confidence: 0.55 (balanced for file organization)
- Filename language: English
- Relate scope: Per-directory
- Organizer timeout: 600 seconds

**Steps:**
1. Ask which .zip file to test (or pick the first one)
2. Ask for test run name (for database file naming)
3. Run the full pipeline: doctor → scan → enrich → dedup → analyze → relate → propose → review (--auto-apply --min-confidence 0.55) → execute (--commit, auto-confirm) → stats
4. Capture results: folder structure, .ddh_trash, database stats, logs
5. Write a critique report covering:
   - Configuration summary
   - Organization quality (folder structure, grouping)
   - Naming quality (descriptiveness, non-ASCII, Hebrew support)
   - Duplicate accuracy (false positives, .ddh_trash quality)
   - Semantic grouping quality (relate step)
   - Error handling & edge cases
   - 3 concrete improvements with rationale

Save the critique to: D:\Test\Test db\<test_name>_critique.md

---

---END---
